# 项目 02：渐进提示

请一次只看一条提示；尝试几分钟后仍然没有思路，再查看下一条。

## 提示 1

先创建一个空列表。在 `for` 循环中读取每个硬度值，并使用列表的 `append()` 方法保存它。

## 提示 2

列表准备好后，可以使用 `sum(列表) / len(列表)` 计算平均值，并使用 `max()` 和 `min()` 找到最大值与最小值。

## 提示 3

极差等于最大硬度减去最小硬度。得到极差后，用 `if` 判断它是否不大于 10。
