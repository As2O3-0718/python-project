# 渐进提示

一次只看一条提示；先自己尝试，再继续下一条。

## 提示 1

先用 `Path(__file__).parent` 得到脚本所在目录，再与输入文件名组合。将所得路径交给 `pandas.read_csv()`，然后查看表格的 `shape`、`columns` 和 `dtypes`，确认读取结果符合任务要求。

## 提示 2

先按 `process` 列进行 `groupby()`，再对 `hardness_hv` 计算数量、平均值和最大值。若要保持原始工艺的出现顺序，可以查看 `groupby()` 中控制排序的参数。

## 提示 3

分组结果需要整理成普通 `DataFrame`，并把统计列改成任务规定的名称。写出时使用 `DataFrame.to_csv()`；注意关闭额外索引列，并在写出前把硬度结果保留一位小数。
