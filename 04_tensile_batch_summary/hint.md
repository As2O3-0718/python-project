# 项目 04：渐进提示

请一次只看一条提示；尝试几分钟后仍然没有思路，再查看下一条。

## 提示 1

可以把一条记录写成类似 `{"sample_id": "S01", "batch": "A", ...}` 的字典，再将多条记录放入同一个列表。

## 提示 2

在 `summarize_batch` 中，先用循环把批次相同的记录收集到新列表；如果新列表为空，就直接返回一个能表示“没有记录”的结果。

## 提示 3

对于筛选出的记录，可以通过字典键读取数值并累加。输出偏低试样时，使用同一条记录中的编号、批次和抗拉强度。
