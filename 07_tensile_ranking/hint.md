# 渐进提示

一次只看一条提示；先自己尝试，再继续下一条。

## 提示 1

先把三个数值列放入一个列名列表中。这个列表既可以用于统计缺失值，也可以用于筛选三个字段都完整的记录；筛选后若要新增列，留意 DataFrame 是否是独立副本。

## 提示 2

屈强比可以直接由两列进行向量化除法得到。达标判断可分别构造两个牌号的完整布尔条件，再将它们合并成 `performance_pass` 列，不需要逐行调用函数。

## 提示 3

分组后可一次聚合数量、平均值和布尔列之和；达标率由达标数与完整试样数计算。排序时按顺序给出三列及各自的升降序设置，完成排序后再生成从 1 开始的排名。
