# Python 材料科学学习仓库规则

## 学习目标

- 面向有 C 语言基础的大学新生，逐步学习 Python，并用于材料科学数据处理。
- 优先保证程序正确、变量含义清楚，再逐步改善 Python 风格。

## 项目结构

- 每个练习使用独立子文件夹，命名为 `两位连续编号_简短英文名称`。
- `task.md` 保存目标、知识点、任务、示例、验收标准和可选挑战，不包含解题提示或答案。
- `hint.md` 单独保存最多三条渐进提示，学习者应一次只查看一条。
- 不预先创建答案代码；数据文件可以随任务提供。

## 教学与验收

- 项目预计用时通常为 30–45 分钟，难度根据上一题表现调整。
- 验收时阅读学习者代码，并运行正常、异常和边界测试。
- 先说明正确之处，再指出会影响正确性的错误，最后给出风格建议。
- 除非学习者明确要求，不直接重写完整答案。
- 对 C 风格习惯给予渐进提醒，例如 `snake_case`、省略条件括号、使用 `return` 和 f-string。

## Git 归档流程

- 只有当天项目明确验收通过后，才更新 `LEARNING_LOG.md` 并归档。
- 提交前检查 `git status` 和实际差异，并执行 `git pull --ff-only`。
- 只暂存 `AGENTS.md`、`LEARNING_LOG.md` 和当天项目文件夹，不使用 `git add .`。
- 不提交密码、令牌、邮箱、设备验证码、虚拟环境或缓存文件。
- 提交信息格式为 `完成项目 NN：中文项目名`，然后推送到 `origin/main`。
- 如有无关改动、冲突、非快进、认证或网络错误，停止推送并报告；不得擅自覆盖或重置。
