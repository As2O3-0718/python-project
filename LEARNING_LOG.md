# Python 材料科学学习记录

## 学习背景

- 有 C 语言基础，熟悉条件、循环和函数。
- 已能使用 pandas 完成长表实验数据的清洗、筛选、分组聚合与排序，使用 NumPy 数组完成广播、向量化计算、按轴统计和布尔筛选，并能用 Matplotlib 绘制材料实验曲线、误差线柱状图、直方图及多子图。

## 已完成项目

### 01：材料密度计算器

- 掌握：输入输出、数值转换、条件判断、函数和格式化输出。
- 完成：计算密度，并判断结果最接近铝、铁还是铜。
- 改进方向：使用正确拼写的变量名，逐步用 f-string 替代字符串拼接。

### 02：维氏硬度数据分析器

- 掌握：列表、`for` 循环、平均值、最大值、最小值和极差。
- 完成：判断测试稳定性，并输出高于平均硬度的记录及序号。
- 测试：正常、波动、非法次数、非法硬度和全部相同数据均通过。
- 改进方向：统一输出中的空格与冒号，逐步采用 `snake_case`。

### 03：热处理温度报告

- 掌握：`pathlib.Path`、相对路径、逐行读取、`strip()`、`enumerate()`、函数多返回值和空数据处理。
- 完成：从 `temperatures.txt` 读取数据，统计平均、最高、最低温度和超温次数，并输出超温序号。
- 测试：正常数据、跨目录启动、小数温度、空数据、850 °C 边界和语法检查均通过。
- 修正：将 `int()` 改为 `float()`，并在分析前处理空列表。
- 改进方向：删除调试输出，继续练习 `snake_case` 和更明确的多返回值解包。

### 04：拉伸实验批次汇总

- 掌握：字典、字典列表、`zip()`、批次筛选、函数多返回值、参数解包和 f-string 数值格式化。
- 完成：汇总 A、B 批次的试样数量、平均与最高抗拉强度、平均断后伸长率，并筛选强度偏低试样；额外完成交互式批次查询。
- 测试：A、B 正常统计、小写查询、不存在批次、465 MPa 边界、无偏低记录、停止查询和语法检查均通过。
- 修正：理解 `dict.fromkeys()` 会为所有键赋同一个值，改用 `dict(zip(keys, values))`；修正函数参数顺序与返回值解包，并处理空批次避免除零。
- 改进方向：继续保持 `snake_case` 和清晰的数据字段命名；后续学习 CSV 结构化数据读写。

### 05：热处理实验 CSV 汇总

- 掌握：pandas 的 `DataFrame` 与 `Series`、`read_csv()`、列类型检查、`groupby()` 分组统计、`unique()`、`reset_index()`、`astype()` 和 `to_csv()`。
- 完成：读取热处理实验 CSV，按工艺汇总试样数量、平均硬度和最高硬度，输出汇总 CSV；额外完成低硬度试样筛选文件。
- 测试：正常数据、跨目录启动、数值列类型、三组统计、首次出现顺序、空数据完整表头、180 HV 筛选边界、CSV 索引和一位小数格式均通过。
- 修正：理解 DataFrame 与 Series 的维度差异及单、双中括号列选择；使用 `orient="index"` 整理嵌套字典方向；接收 `reset_index()` 与 `astype()` 的返回值，并正确控制 CSV 的索引和浮点格式。
- 改进方向：减少仅为展示信息而编写的手动循环，继续练习 pandas 的聚合、缺失值处理和更紧凑的表格操作。

### 06：合金成分数据清洗与质量汇总

- 掌握：pandas 缺失值统计、按指定列进行布尔筛选、逐行 `apply()`、布尔结果列、`groupby()` 分组统计、`Series.map()` 索引映射、Series 对齐运算和数值舍入。
- 完成：筛选成分与硬度字段完整的有效试样，判断 Steel-A、Steel-B 成分是否合格，按牌号汇总有效数量、平均硬度、合格数量和合格率，并输出有效样本与质量汇总 CSV；额外完成缺失记录及缺失字段数输出。
- 测试：正常数据、跨目录启动、三列缺失值统计、空输入完整表头、上下边界、范围外数据、首次出现顺序、非整齐小数格式和非数值字段缺失均通过。
- 修正：理解布尔 Series 的整体判断与索引对齐；使用 `map()` 将分组结果按合金名称映射到结果表；将有效样本原始列与内部合格判断列分开输出，并分别处理原始数据精度、汇总一位小数和空结果打印。
- 改进方向：布尔筛选后需要继续赋值时可显式使用 `.copy()`；可继续尝试用 pandas 向量化条件减少仅用于逐行判断或打印的 `apply()`。

### 07：拉伸试验批次排名

- 掌握：pandas 向量化多条件判断、屈强比计算、`groupby().agg()` 命名聚合、布尔值求和、多条件排序、`DataFrame.insert()`、浮点输出格式和空 DataFrame 处理。
- 完成：清洗拉伸试验数据，按牌号和批次汇总完整试样数、平均抗拉强度、平均断后伸长率、达标数与达标率，并按达标率、平均抗拉强度和批次名称生成排名；额外输出未达标试样及失败原因。
- 测试：正常数据、跨目录启动、缺失值筛选、精确上下边界、刚超出屈强比边界、三层排序、CSV 两位小数格式、空输入完整表头和空表终端输出均通过。
- 修正：计算屈强比时保留原始精度，只在输出阶段格式化，避免四舍五入后误判；在使用逐行 `apply()` 打印前处理空表，避免 pandas 为推断返回类型而调用函数产生虚假 `NaN` 输出。
- 改进方向：继续区分“计算精度”和“显示格式”，理解向量化、聚合与逐行应用的不同用途；执行纯打印等副作用操作时优先考虑语义明确的循环。

### 08：热膨胀测量 NumPy 分析

- 掌握：NumPy 一维与二维数组、`shape`、行列索引与切片、广播规则、向量化运算、`axis`、布尔筛选、`np.diff()`、`any()`、`mean()` 和总体标准差。
- 完成：计算多件试样相对于初始长度的变化量和线膨胀系数，筛选合格试样，统计各温度平均长度，并完成严格单调增长检查的可选挑战。
- 测试：正常输出、挑战异常试样筛选、语法检查、数组维度与数量错误、温度不足、温度重复或下降、初始长度为零或负数、合格范围上下边界和严格递增等号边界均通过。
- 修正：理解一维 `(4,)` 与二维列 `(4, 1)` 的广播差异；使用 `any(axis=1)` 汇总逐行布尔结果；接收 `np.delete()` 返回的新数组；使用 `join()` 将 NumPy 字符串数组格式化为指定输出。
- 改进方向：继续使用复数变量名表示数组集合，并在多维运算中显式标明 `axis`；后续任务避免为主任务与挑战复制大段硬编码数据，优先通过函数参数或派生数组复用数据。

### 09：拉伸试验应力—应变曲线可视化

- 掌握：Matplotlib 的 `Figure` 与 `Axes`、折线和散点、标题与坐标轴单位、网格、图例、`annotate()` 峰值标注、`axvspan()` 区间色带、透明度、图像尺寸与 DPI；继续练习 NumPy 峰值索引和布尔筛选。
- 完成：从 CSV 读取退火态与调质态拉伸测量数据，在同一坐标轴绘制两条工程应力—应变曲线，计算并突出显示峰值，保存完整 PNG；额外计算两条曲线不低于峰值 98% 的应变区间，并添加半透明色带及图例。
- 测试：正常输出、从仓库根目录和项目目录启动、峰值计算、98% 精确边界、空数组、缺少数据列、语法检查、无意义空白检查、PNG 路径、`1200 × 750` 像素尺寸及图像目视检查均完成。
- 修正：将输出路径从当前工作目录改为项目文件夹，避免图片误存仓库根目录；将峰值应力格式化为零位小数，并补充独立散点以突出峰值。
- 改进方向：单个坐标轴变量可使用单数 `ax`；可用 `fig.tight_layout()` 和 `fig.savefig()` 明确由整张图负责布局与保存，并用 `data[row, column]` 简化二维数组索引。

### 10：热处理硬度统计子图

- 掌握：长表实验数据结构、`groupby(sort=False)` 首次出现顺序、命名聚合、总体标准差、`idxmax()`、Matplotlib 一行两列子图、误差线柱状图、分类刻度、柱顶标注、直方图固定分箱、`MaxNLocator` 整数刻度、阈值线和图例锚点定位。
- 完成：从 CSV 动态读取不同温度和不等组内数量的硬度记录，输出分组统计并突出最高平均硬度组，在同一 Figure 中绘制平均硬度柱状图与总体硬度直方图；额外统计不低于 190 HV 的试样比例并添加阈值线和图例。
- 测试：正常输出、从仓库根目录和项目目录启动、不等组内数量、单试样组总体标准差、均值并列时首次出现组优先、缺少必需列、语法检查、无意义空白检查、PNG 路径、`1500 × 675` 像素尺寸及图像目视检查均完成。
- 修正：将固定试样列改为一条记录一行的长表输入；用连续类别位置改善柱宽与间距；修正温度和最高均值格式；保留分组首次出现顺序；为最高柱设置独立颜色；将网格改为虚线，并把挑战图例移到坐标轴外避免遮挡标题和数据。
- 改进方向：避免用 `DataFrame.apply()` 执行打印副作用；可进一步让分析函数只返回结果、由主流程统一输出，并用更贴合职责的名称替代 `print_plot()`。

### 11：拉伸弹性模量线性拟合

- 掌握：pandas 弹性应变区间筛选、`numpy.polynomial.Polynomial.fit()` 缩放坐标拟合、`convert()` 原始坐标系系数转换、弹性模量单位换算、预测值、残差、决定系数 `R²`、最大绝对残差定位，以及 Matplotlib 上下子图绘制。
- 完成：从长表 CSV 读取拉伸测量点，拟合 `0.0000–0.0025` 弹性区并得到 `69.37 GPa` 弹性模量；绘制完整应力—应变曲线、拟合点、拟合直线和残差图；额外完成 `R²`、最大绝对残差统计与图中标注。
- 测试：正常输出、从仓库根目录和项目目录启动、零点、单点、两个不同边界点、多个相同应变点、语法检查、无意义空白检查、PNG 路径、`1500 × 1050` 像素尺寸及图像目视检查均通过。
- 修正：理解 `Polynomial.fit()` 返回缩放坐标系中的系数，并用 `convert()` 获取具有物理意义的截距和斜率；改用各 `Axes` 对象的方法将内容画到指定子图；增加不同应变数量检查，避免病态拟合产生无意义斜率；将最大残差输出移回主流程，并清理三处无意义行尾或空白行残留空格。
- 改进方向：异常信息可进一步区分“测量点不足”和“缺少两个不同应变值”；可减少对同一弹性区条件的重复筛选，并让绘图函数只负责绘图与保存、由主流程统一报告输出。

### 12：Arrhenius 扩散系数 SciPy 拟合

- 掌握：Arrhenius 关系线性化、摄氏温度到开尔文温度换算、`1000 / T` 与自然对数变换、`scipy.stats.linregress()` 回归结果、斜率标准误、t 临界值、95% 置信区间、扩散活化能和指前因子计算。
- 完成：从长表 CSV 读取碳扩散实验数据，在变换变量空间完成 SciPy 线性回归，输出活化能 `152.1 kJ/mol`、95% 置信区间 `143.7–160.4 kJ/mol`、指前因子和 `R²`，并绘制 Arrhenius 散点与拟合直线；额外预测 `1000 °C` 扩散系数并在图中突出预测点。
- 测试：正常输出、从仓库根目录和项目目录启动、三个必需列分别缺失、单元格缺失、绝对零度及以下、零或负扩散系数、少于三个不同温度点、语法检查、无意义空白检查、拟合线与预测范围、虚线网格、三项图例、PNG 路径、`1200 × 900` 像素尺寸及图像目视检查均通过。
- 修正：区分“整列缺失”和“单元格缺失”，使用集合差集检查 CSV 表头；修正 `ValueError` 构造、终端区间排版、科学计数法精度和网格线样式；理解 Windows 终端编码差异，并使用 Python UTF-8 模式完成包含 `D₀` 与 `R²` 的输出测试。
- 延伸理解：了解 `linregress()` 返回值及相关系数、p 值、标准误的日常含义，理解 t 临界值与置信区间宽度；掌握 Matplotlib 通过解析式采样绘图、拟合线显示范围控制、集合与字典的语法区别，并了解结果字典、`print(..., sep="\n")` 和类似 C 语言 `struct` 的 `dataclass` 可用于组织多项分析结果。
- 改进方向：函数输出按职责安排，纯分析函数可返回结果，保存图片或导出文件的函数可以就地报告完成信息；当稳定结果字段较多且需要在多个函数间传递时，再从字典自然过渡到 `dataclass`。变量可使用 `gas_constant`、`activation_energy` 等清楚名称，并删除未使用的中间结果。

### 13：Arrhenius 非线性拟合与残差

- 掌握：`scipy.optimize.curve_fit()` 非线性拟合、极小扩散系数的数值缩放、指前因子与活化能的单位换算、实验观测值与模型预测值、残差和 RMSE，以及 Matplotlib 对数纵轴与共享横轴子图。
- 完成：从长表 CSV 读取 8 条扩散实验记录，在缩放数值空间拟合 Arrhenius 非线性关系，得到活化能 `153.7 kJ/mol`、指前因子 `4.391e-05 m²/s` 和 RMSE `1.424e-13 m²/s`；额外完成线性化拟合，比较两种方法的参数、残差和 RMSE，并绘制两条拟合曲线与两组残差。
- 测试：正常输出、从仓库根目录和项目目录启动、三个必需列缺失、单元格缺失、绝对零度及以下、零或负扩散系数、少于及恰好三个不同温度点、语法检查、无意义空白检查、PNG 路径、`1200 × 1050` 像素尺寸及图像目视检查均通过。
- 修正：将活化能从 `J/mol` 正确换算为 `kJ/mol`；在线性化拟合中先用 `exp()` 将对数预测还原为 `m²/s` 再计算 RMSE；理清缩放指前因子与物理单位指前因子的对应关系；删除调试数组输出，并补齐固定标题、项目目录 PNG 保存和成功信息。
- 改进方向：使用带单位和数据角色的变量名，明确区分 `measured`、`predicted`、`scaled`、`m2_s`、`j_mol` 与 `kj_mol`；函数名继续向严格的全小写 `snake_case` 调整，较长公式可拆成有单位含义的中间变量。输入检查还可逐步增加非数字和无穷值处理。

## 下一步

- 继续保持“计算使用原始精度、显示阶段格式化”的习惯，并按实际职责组织数据检查、分析、输出和绘图，不机械集中所有打印信息。
- 下一题适当降低新物理概念密度，优先在较直观的材料数据场景中巩固 SciPy、残差和可视化，并在题面首次使用术语时先解释含义与单位。
- 在后续材料科学任务中自然练习跨文件模块组织与异常处理；当结果字段稳定且需要反复传递时，可用带显式单位字段的 `dataclass` 替代含义模糊的通用字典键，但不一次引入过多新概念。
